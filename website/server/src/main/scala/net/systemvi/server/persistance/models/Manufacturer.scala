package net.systemvi.server.persistance.models

import java.util.UUID

case class Manufacturer(
                         uuid:UUID,
                         name:String
                       )


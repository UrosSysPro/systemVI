package com.systemvi.ray_marching

import cats.*
import cats.implicits.*
import cats.effect.*
import cats.effect.implicits.*

object Main extends IOApp.Simple {
  override def run: IO[Unit] = {
  
    IO.println("hello")
  }
}